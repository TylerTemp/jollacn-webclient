## jollacn ##

```bash
yarn install
yarn add --global npx
```

*   to start: `yarn start`
*   to build: `yarn build`

```bash
NODE_OPTIONS=--openssl-legacy-provider yarn start --env API=https://notexists.top
```